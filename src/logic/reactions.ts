// import { Reaction } from 'overmind';
// import * as actions from './actions';

// export const updateHitsOnQueryChange: Reaction = (reaction, action) =>
//   reaction(state => state.query, actions.updateHits(action));
