import styled from 'react-emotion';
import ColorBox from './ColorBox';

const Header = styled(ColorBox)({
  height: 50,
  padding: '10px 20px',
  alignItems: 'center',
  textTransform: 'uppercase',
  fontWeight: 400,
});

export default Header;
